


# Standard MQTT listener for the ESP32
listener 1883 0.0.0.0
protocol mqtt

# WebSocket listener (Optional, but good for future-proofing)
listener 9001 0.0.0.0
protocol websockets

# Allow connections without a username/password
allow_anonymous true
# persistence true
# persistence_location /mosquitto/data/
# log_dest stdout